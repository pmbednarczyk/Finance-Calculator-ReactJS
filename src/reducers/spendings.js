/*
const spendings = (state = [], action) => {
  switch (action.type) {
    case 'ADD_SPENDING':
      return [
        ...state,
        {
          id: action.id,
          text: action.text,
          completed: false,
        },
      ];
  }
};

export default spendings;
*/
